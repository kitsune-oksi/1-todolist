export enum ETaskStatuses {
  New = 0,
  InProgress = 1,
  Completed = 2,
  Draft = 3,
}

export enum ETaskPriorities {
  Low = 0,
  Middle = 1,
  Hight = 2,
  Urgently = 3,
  Later = 4,
}

export enum EResultCode {
  success = 0,
  error = 1,
  captcha = 10,
}
